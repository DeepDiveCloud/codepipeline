Resources:
  CICDVPC:
    Type: AWS::EC2::VPC
    Properties:
      CidrBlock: 10.0.0.0/16      

  IGW:
    Type: AWS::EC2::InternetGateway

  IGW-Attachment:
    Type: AWS::EC2::VPCGatewayAttachment
    Properties:
      VpcId: !Ref CICDVPC
      InternetGatewayId: !Ref IGW

  Pub-Subnet:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref CICDVPC
      CidrBlock: 10.0.0.0/24
      AvailabilityZone: "ap-south-1b"
      MapPublicIpOnLaunch: true  

  Pub-RouteTable:
    Type: AWS::EC2::RouteTable
    Properties:
      VpcId: !Ref CICDVPC

  Pub-Route:
    Type: AWS::EC2::Route
    DependsOn: IGW-Attachment
    Properties:
      RouteTableId: !Ref Pub-RouteTable
      DestinationCidrBlock: 0.0.0.0/0
      GatewayId: !Ref IGW

  SubnetAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      SubnetId: !Ref Pub-Subnet
      RouteTableId: !Ref Pub-RouteTable

  Inst-SecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Allow HTTP & SSH access
      VpcId: !Ref CICDVPC
      SecurityGroupIngress:
        - CidrIp: 0.0.0.0/0
          IpProtocol: tcp
          FromPort: 80
          ToPort: 80
        - CidrIp: 0.0.0.0/0
          IpProtocol: tcp
          FromPort: 8080
          ToPort: 8080
        - CidrIp: 0.0.0.0/0
          IpProtocol: tcp
          FromPort: 22
          ToPort: 22
      
  WebServer:
    Type: AWS::EC2::Instance
    Properties:
      ImageId: ami-049a62eb90480f276 
      InstanceType: t2.micro
      KeyName: "ci-cd"
      SecurityGroupIds:
        - !Ref Inst-SecurityGroup
      SubnetId: !Ref Pub-Subnet
      Tags:
        - Key: Name
          Value: PipelineServer
      UserData:
        Fn::Base64: !Sub |
          #!/bin/bash
          sudo yum update -y
          sudo yum install nginx -y
          sudo systemctl start nginx          

  MyS3Bucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: "DDCloud-Ranjith"

